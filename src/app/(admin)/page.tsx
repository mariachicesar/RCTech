// This route group previously contained a page. To avoid manifest conflicts
// with /admin/page.tsx, this file intentionally exports no page.
// If you want (admin) group to have its own page again, prefer /admin/page.tsx only.
export default function GroupStub() {
  return null;
}
